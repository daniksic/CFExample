<style>
    .galleria {
        min-width: 470px;
        min-height: 500px;
        background: #fff;
    }
</style>
<div class="container">
    <div class="row">
        <header class="page-header col-md-12">
            <h2>Photobook</h2>
        </header>
        <article class="col-md-12">
            <figure>
                <div class="galleria hidden-xs hidden-sm" id="galleria">
                </div>
                <div class="hidden-lg hidden-md well">
                    <p class="text-danger text-center">Please view this page on a screen with a bigger resolution to open up the gallery pictures.<br />Thank you!</p>
                </div>
            </figure>
        </article>
    </div>
    <hr />
    @*@if (User.Identity.IsAuthenticated)
    {
        <div class="row">
            <div class="col-md-12">
                <h2>Comment</h2>
                @using (Html.BeginForm("SaveComment", "Home", FormMethod.Post, new { @class = "form-horizontal", @role = "form" }))
                {
                    @Html.AntiForgeryToken();
                    @Html.Hidden("name",User.Identity.Name)
                    <div class="form-group">
                        <label for="inputName" class="col-sm-2 control-label">Name:</label>
                        <div class="col-sm-10">
                            <label for="name">@User.Identity.Name</label>
                            <input type="text" name="name" id="name" class="form-control" value="@User.Identity.Name" required="required" disabled>
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="inputEmail" class="col-sm-2 control-label">E-mail:</label>
                        <div class="col-sm-10">
                            <input type="email" name="email"  id="email" class="form-control" placeholder="E-mail" required="required">
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="inputMessage" class="col-sm-2 control-label">Comment:</label>
                        <div class="col-sm-10">
                            <textarea name="comment" id="comment" rows="5" class="form-control" placeholder="Enter your comment here" required="required"></textarea>
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-sm-offset-2 col-sm-10">
                            <button type="submit" class="btn btn-success">Comment</button>
                        </div>
                    </div>

                }
            </div>
        </div>
    }
    else
    {
        <div class="row">
            <div class="col-md-12">
                <h2>Comment</h2>
                <div class="well text-center">
                    <p>Please <a href="~/Account/Login">sign in or register</a> to post a comment</p>
                </div>
            </div>
        </div>
    }*@
    @*<div class="row">
        <div class="col-md-12">
            @Html.Partial("_Comments")
        </div>
    </div>*@
</div>
@section scripts{
    @Html.Partial("_GalleryPictures")
}